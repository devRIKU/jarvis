1. Set up Asteryx styles in `src/main.tsx`: import `@astryxdesign/core/reset.css`, `@astryxdesign/core/astryx.css`, and wrap the app in `<Theme theme={neutralTheme}>` using `import { neutralTheme } from '@astryxdesign/theme-neutral/built'`. Then verify `src/main.tsx` with `read_file`.
2. Implement the Chat UI in `src/App.tsx`. Replace the current contents with a `Layout` container, a `ChatMessageList` to show messages, and a `ChatComposer` at the bottom. Use a React state variable `messages` to store the chat history. Implement an API Key input so the user can provide their own key. Verify the file changes with `read_file`.
3. Add text API logic to `src/App.tsx`. Import `GoogleGenAI` from `@google/genai`. Initialize the `GoogleGenAI` client using the provided API key state. Implement an `onSubmit` handler in `ChatComposer` that checks if the message starts with `/image`. If not, send the text to a text model using `ai.models.generateContent` and append the text response to `messages`. Verify with `read_file`.
4. Add image API logic to `src/App.tsx`. Update the `onSubmit` handler so if the message starts with `/image`, it calls `ai.models.generateImages` to generate an image. Check if `response.generatedImages?.[0]?.image?.imageBytes` exists and store the returned string (base64) in the chat history, then display it using an `<img>` inside `ChatMessageBubble`. Verify with `read_file`.
5. Run tests or build checks to ensure no regressions were introduced by running `npm run build`.
6. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
7. Submit changes with a descriptive commit message.
